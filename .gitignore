.DS_Store
*.xcuserstate
xcuserdata/
.claude/
build/

# Local build-time secrets (Withings dev-app credentials). The committed
# Config/Base.xcconfig + Config/Secrets.example.xcconfig describe the pattern.
Config/Secrets.xcconfig

# Cloudflare Wrangler local cache/tmp (reports-worker)
.wrangler/

# reports-worker npm deps (restore with `npm install`)
reports-worker/node_modules/

# community-worker npm deps (restore with `npm install`)
community-worker/node_modules/

# Local release build (scripts/build-macos-release.sh)
.build-macos-release/
dist/
