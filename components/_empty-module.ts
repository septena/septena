// Placeholder for optional local-only overlay modules. next.config.ts
// aliases missing `*.local` imports here so the bundler resolves to an
// empty module instead of failing the build.
export {};
